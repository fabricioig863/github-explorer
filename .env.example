# Use in-memory mocks instead of real GitHub API (default: true)
EXPO_PUBLIC_USE_MOCK=true

# Optional. Personal access token used to raise the GitHub REST rate limit
# from 60 to 5000 requests/hour for unauthenticated/authenticated calls.
# Leave blank to use the unauthenticated quota.
GITHUB_TOKEN=
